[package]
name = "nvidia-driver-slint-gui"
version = "0.1.0"
edition = "2021"

[dependencies]
slint = { version = "=1.7.2", default-features = false, features = ["std", "backend-winit", "renderer-software", "compat-1-2"] }

[build-dependencies]
slint-build = "=1.7.2"
