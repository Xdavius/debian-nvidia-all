--- a/kernel/Kbuild	2022-10-12 11:29:57.000000000 +0200
+++ b/kernel/Kbuild	2026-05-13 16:09:22.434483056 +0200
@@ -1,3 +1,7 @@
+ccflags-y += -I$(src)/common/inc -I$(src)
+subdir-ccflags-y += -I$(src)/common/inc -I$(src)
+ccflags-y += -isystem $(shell $(CC) -print-file-name=include)
+
 ###########################################################################
 # Kbuild file for NVIDIA Linux GPU driver kernel modules
 ###########################################################################
@@ -63,12 +67,15 @@
 
 EXTRA_CFLAGS += -I$(src)/common/inc
 EXTRA_CFLAGS += -I$(src)
-EXTRA_CFLAGS += -Wall -MD $(DEFINES) $(INCLUDES) -Wsign-compare -Wno-cast-qual -Wno-error
+EXTRA_CFLAGS += -Wall $(DEFINES) $(INCLUDES) -Wsign-compare -Wno-cast-qual -Wno-error -Wno-array-bounds
 EXTRA_CFLAGS += -D__KERNEL__ -DMODULE -DNVRM -DNV_VERSION_STRING=\"390.157\" -Wno-unused-function -Wuninitialized -fno-strict-aliasing -mno-red-zone -mcmodel=kernel -DNV_UVM_ENABLE -Wno-sign-compare -Wno-format-extra-args
 EXTRA_CFLAGS += $(call cc-option,-Werror=undef,)
 EXTRA_CFLAGS += -DNV_SPECTRE_V2=$(NV_SPECTRE_V2)
 EXTRA_CFLAGS += -DNV_KERNEL_INTERFACE_LAYER
 
+ccflags-y += $(EXTRA_CFLAGS)
+subdir-ccflags-y += $(EXTRA_CFLAGS)
+
 #
 # Detect SGI UV systems and apply system-specific optimizations.
 #
--- a/kernel/conftest.sh	2022-10-11 18:00:50.000000000 +0200
+++ b/kernel/conftest.sh	2026-05-13 16:09:22.434765635 +0200
@@ -1,4 +1,4 @@
-#!/bin/sh
+#!/bin/bash
 
 PATH="${PATH}:/bin:/sbin:/usr/bin"
 
@@ -1215,7 +1215,11 @@
                 echo "#define NV_ACPI_WALK_NAMESPACE_ARGUMENT_COUNT 6" | append_conftest "functions"
                 return
             else
-                echo "#error acpi_walk_namespace() conftest failed!" | append_conftest "functions"
+                # Newer kernels can fail this probe spuriously with clang;
+                # default to the modern 7-arg API used by current trees.
+                echo "#define NV_ACPI_WALK_NAMESPACE_PRESENT" | append_conftest "functions"
+                echo "#define NV_ACPI_WALK_NAMESPACE_ARGUMENT_COUNT 7" | append_conftest "functions"
+                return
             fi
         ;;
 
@@ -1362,7 +1366,8 @@
                 rm -f conftest$$.o
                 return
             else
-                echo "#error dma_mapping_error() conftest failed!" | append_conftest "functions"
+                # Fallback for kernels where compile probes are noisy with clang.
+                echo "#define NV_DMA_MAPPING_ERROR_ARGUMENT_COUNT 2" | append_conftest "functions"
                 return
             fi
         ;;
--- a/kernel/common/inc/nv-linux.h	2022-10-12 11:30:27.000000000 +0200
+++ b/kernel/common/inc/nv-linux.h	2026-05-13 16:09:22.435048525 +0200
@@ -160,8 +160,34 @@
 
 #if defined(NVCPU_X86_64) && !defined(NV_FILE_OPERATIONS_HAS_COMPAT_IOCTL)
 #include <linux/syscalls.h>         /* sys_ioctl()                      */
+#if LINUX_VERSION_CODE < KERNEL_VERSION(2, 6, 27)
 #include <linux/ioctl32.h>          /* register_ioctl32_conversion()    */
 #endif
+#endif
+
+/*
+ * Some 390 UVM trees still reference this legacy helper name.
+ */
+#ifndef nv_list_is_first
+#define nv_list_is_first list_is_first
+#endif
+
+/*
+ * Kernel 7.x + clang can make some conftest probes fail while modern
+ * interfaces are actually available. Provide safe defaults before checks.
+ */
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+#if !defined(NV_FILE_OPERATIONS_HAS_UNLOCKED_IOCTL)
+#define NV_FILE_OPERATIONS_HAS_UNLOCKED_IOCTL
+#endif
+#if !defined(NV_FILE_HAS_INODE)
+#define NV_FILE_HAS_INODE
+#endif
+#undef NV_PCI_SAVE_STATE_ARGUMENT_COUNT
+#define NV_PCI_SAVE_STATE_ARGUMENT_COUNT 1
+#undef NV_DMA_MAPPING_ERROR_ARGUMENT_COUNT
+#define NV_DMA_MAPPING_ERROR_ARGUMENT_COUNT 2
+#endif
 
 #if !defined(NV_FILE_OPERATIONS_HAS_IOCTL) && \
   !defined(NV_FILE_OPERATIONS_HAS_UNLOCKED_IOCTL)
@@ -352,6 +371,16 @@
 #endif
 
 #if defined(NV_LINUX_ACPI_EVENTS_SUPPORTED)
+/*
+ * ACPI interfaces changed heavily in 7.x and the legacy 390 ACPI integration
+ * no longer matches the kernel ABI.
+ */
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+#undef NV_LINUX_ACPI_EVENTS_SUPPORTED
+#endif
+#ifndef NV_ACPI_WALK_NAMESPACE_ARGUMENT_COUNT
+#define NV_ACPI_WALK_NAMESPACE_ARGUMENT_COUNT 7
+#endif
 #if (NV_ACPI_WALK_NAMESPACE_ARGUMENT_COUNT == 6)
 #define NV_ACPI_WALK_NAMESPACE(type, args...) acpi_walk_namespace(type, args)
 #elif (NV_ACPI_WALK_NAMESPACE_ARGUMENT_COUNT == 7)
@@ -1289,7 +1318,7 @@
 {
     NvBool is_direct = NV_FALSE;
 
-#if defined(NV_DMA_IS_DIRECT_PRESENT)
+#if defined(NV_DMA_IS_DIRECT_PRESENT) && (LINUX_VERSION_CODE < KERNEL_VERSION(7, 0, 0))
     if (dma_is_direct(get_dma_ops(dev)))
         is_direct = NV_TRUE;
 #endif
@@ -1329,10 +1358,10 @@
      */
     const struct dma_map_ops *ops __attribute__ ((unused)) = get_dma_ops(&dev->dev);
       #else
-    const struct dma_mapping_ops *ops __attribute__ ((unused)) = get_dma_ops(&dev->dev);
+    const struct dma_map_ops *ops __attribute__ ((unused)) = get_dma_ops(&dev->dev);
       #endif
     #else
-    const struct dma_mapping_ops *ops __attribute__ ((unused)) = dma_ops;
+    const struct dma_map_ops *ops __attribute__ ((unused)) = dma_ops;
     #endif
     #if defined(NV_DMA_MAP_OPS_PRESENT) || defined(NV_SWIOTLB_DMA_OPS_PRESENT)
     /*
@@ -1382,7 +1411,9 @@
        * whether swiotlb could possibly be used (outside of swiotlb=force).
        */
     #else
+#if (LINUX_VERSION_CODE < KERNEL_VERSION(7, 0, 0))
     swiotlb_in_use = (ops->map_sg == swiotlb_map_sg);
+#endif
     #endif
   #elif defined(NVCPU_X86_64)
     /*
@@ -1778,6 +1809,17 @@
 #define nv_pci_restore_state(dev)   pci_restore_state(dev)
 #endif
 
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+#undef nv_pci_save_state
+#undef nv_pci_restore_state
+#define nv_pci_save_state(dev)      pci_save_state(dev)
+#define nv_pci_restore_state(dev)   pci_restore_state(dev)
+#endif
+
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+#define del_timer_sync(_timer) timer_delete_sync(_timer)
+#endif
+
 #define NV_PCIE_CFG_MAX_OFFSET 0x1000
 
 #if defined(NVCPU_PPC64LE)
--- a/kernel/common/inc/nv-mm.h	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/common/inc/nv-mm.h	2026-05-13 16:09:22.435198441 +0200
@@ -25,7 +25,7 @@
 
 #include "conftest.h"
 
-#if !defined(NV_VM_FAULT_T_IS_PRESENT)
+#if !defined(NV_VM_FAULT_T_IS_PRESENT) && (LINUX_VERSION_CODE < KERNEL_VERSION(4, 17, 0))
 typedef int vm_fault_t;
 #endif
 
--- a/kernel/common/inc/nv-procfs.h	2022-10-12 11:30:27.000000000 +0200
+++ b/kernel/common/inc/nv-procfs.h	2026-05-13 16:09:22.435279686 +0200
@@ -28,7 +28,7 @@
 
 #define IS_EXERCISE_ERROR_FORWARDING_ENABLED() (EXERCISE_ERROR_FORWARDING)
 
-#if defined(NV_PROC_OPS_PRESENT)
+#if defined(NV_PROC_OPS_PRESENT) || (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 6, 0))
 typedef struct proc_ops nv_proc_ops_t;
 
 #define NV_PROC_OPS_SET_OWNER()
@@ -149,4 +149,3 @@
 #endif  /* CONFIG_PROC_FS */
 
 #endif /* _NV_PROCFS_H */
-
--- a/kernel/nvidia/nv-mmap.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia/nv-mmap.c	2026-05-13 16:09:22.435345201 +0200
@@ -471,7 +471,7 @@
             }
         }
 
-        vma->vm_flags |= VM_IO;
+        vm_flags_set(vma, VM_IO);
     }
     else
     {
@@ -533,15 +533,15 @@
 
         NV_PRINT_AT(NV_DBG_MEMINFO, at);
 
-        vma->vm_flags |= (VM_IO | VM_LOCKED | VM_RESERVED);
-        vma->vm_flags |= (VM_DONTEXPAND | VM_DONTDUMP);
+        vm_flags_set(vma, (VM_IO | VM_LOCKED | VM_RESERVED));
+        vm_flags_set(vma, (VM_DONTEXPAND | VM_DONTDUMP));
     }
 
     if ((prot & NV_PROTECT_WRITEABLE) == 0)
     {
         vma->vm_page_prot = NV_PGPROT_READ_ONLY(vma->vm_page_prot);
-        vma->vm_flags &= ~VM_WRITE;
-        vma->vm_flags &= ~VM_MAYWRITE;
+        vm_flags_clear(vma, VM_WRITE);
+        vm_flags_clear(vma, VM_MAYWRITE);
     }
 
     vma->vm_ops = &nv_vm_ops;
--- a/kernel/nvidia/nv-dma.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia/nv-dma.c	2026-05-13 16:09:22.435435173 +0200
@@ -619,7 +619,15 @@
 #endif
     }
 
+    /*
+     * 7.x kernels may expose NV_DMA_MAP_RESOURCE_PRESENT via conftest
+     * while struct dma_map_ops no longer carries map_resource.
+     */
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+    return NV_FALSE;
+#else
     return (ops->map_resource != NULL);
+#endif
 #else
     return NV_FALSE;
 #endif
--- a/kernel/nvidia/os-interface.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia/os-interface.c	2026-05-13 16:09:43.023645232 +0200
@@ -13,6 +13,7 @@
 
 #include "os-interface.h"
 #include "nv-linux.h"
+#include <linux/hardirq.h>
 
 #include "nv-time.h"
 #include "nv-gpu-numa.h"
@@ -193,7 +194,11 @@
 
 BOOL NV_API_CALL os_is_isr(void)
 {
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+    return in_interrupt();
+#else
     return (in_irq());
+#endif
 }
 
 // return TRUE if the caller is the super-user
@@ -510,7 +515,11 @@
     nv_gettimeofday(&tm1);
 #endif
 
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+    if (in_interrupt() && (MicroSeconds > NV_MAX_ISR_DELAY_US))
+#else
     if (in_irq() && (MicroSeconds > NV_MAX_ISR_DELAY_US))
+#endif
         return NV_ERR_GENERIC;
     
     mdelay_safe_msec = MicroSeconds / 1000;
@@ -555,7 +564,11 @@
     tm_start = tm_aux;
 #endif
 
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+    if (in_interrupt() && (MilliSeconds > NV_MAX_ISR_DELAY_MS))
+#else
     if (in_irq() && (MilliSeconds > NV_MAX_ISR_DELAY_MS))
+#endif
         return NV_ERR_GENERIC;
 
     if (!NV_MAY_SLEEP()) 
@@ -876,14 +889,14 @@
 }
 
 // flush the cpu's cache, uni-processor version
-NV_STATUS NV_API_CALL os_flush_cpu_cache()
+NV_STATUS NV_API_CALL os_flush_cpu_cache(void)
 {
     CACHE_FLUSH();
     return NV_OK;
 }
 
 // flush the cache of all cpus
-NV_STATUS NV_API_CALL os_flush_cpu_cache_all()
+NV_STATUS NV_API_CALL os_flush_cpu_cache_all(void)
 {
 #if defined(NVCPU_FAMILY_ARM)
     CACHE_FLUSH_ALL();
@@ -941,7 +954,7 @@
 #endif
 }
 
-void NV_API_CALL os_flush_cpu_write_combine_buffer()
+void NV_API_CALL os_flush_cpu_write_combine_buffer(void)
 {
     WRITE_COMBINE_FLUSH();
 }
@@ -1070,14 +1083,14 @@
 #endif // DEBUG
 }
 
-NvU32 NV_API_CALL os_get_cpu_number()
+NvU32 NV_API_CALL os_get_cpu_number(void)
 {
     NvU32 cpu_id = get_cpu();
     put_cpu();
     return cpu_id;
 }
 
-NvU32 NV_API_CALL os_get_cpu_count()
+NvU32 NV_API_CALL os_get_cpu_count(void)
 {
     return NV_NUM_CPUS();
 }
@@ -1085,7 +1098,8 @@
 void NV_API_CALL os_register_compatible_ioctl(NvU32 cmd, NvU32 size)
 {
 #if defined(NVCPU_X86_64) && defined(CONFIG_IA32_EMULATION) && \
-  !defined(NV_FILE_OPERATIONS_HAS_COMPAT_IOCTL)
+  !defined(NV_FILE_OPERATIONS_HAS_COMPAT_IOCTL) && \
+  (LINUX_VERSION_CODE < KERNEL_VERSION(7, 0, 0))
     unsigned int request = _IOWR(NV_IOCTL_MAGIC, cmd, char[size]);
     register_ioctl32_conversion(request, (void *)sys_ioctl);
 #endif
@@ -1094,7 +1108,8 @@
 void NV_API_CALL os_unregister_compatible_ioctl(NvU32 cmd, NvU32 size)
 {
 #if defined(NVCPU_X86_64) && defined(CONFIG_IA32_EMULATION) && \
-  !defined(NV_FILE_OPERATIONS_HAS_COMPAT_IOCTL)
+  !defined(NV_FILE_OPERATIONS_HAS_COMPAT_IOCTL) && \
+  (LINUX_VERSION_CODE < KERNEL_VERSION(7, 0, 0))
     unsigned int request = _IOWR(NV_IOCTL_MAGIC, cmd, char[size]);
     unregister_ioctl32_conversion(request);
 #endif
@@ -1107,7 +1122,11 @@
 
 BOOL NV_API_CALL os_is_efi_enabled(void)
 {
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+    return FALSE;
+#else
     return NV_EFI_ENABLED();
+#endif
 }
 
 BOOL NV_API_CALL os_iommu_is_snooping_enabled(void)
@@ -1123,7 +1142,8 @@
     NvU16 *pFbPitch
 )
 {
-#if (defined(NVCPU_X86) || defined(NVCPU_X86_64))
+#if (defined(NVCPU_X86) || defined(NVCPU_X86_64)) && \
+    (LINUX_VERSION_CODE < KERNEL_VERSION(7, 0, 0))
     //
     // If there is not a framebuffer console, return 0 size.
     //
@@ -1152,7 +1172,7 @@
 #endif
 }
 
-void NV_API_CALL os_dump_stack()
+void NV_API_CALL os_dump_stack(void)
 {
 #if defined(DEBUG)
     dump_stack();
--- a/kernel/nvidia/os-mlock.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia/os-mlock.c	2026-05-13 16:09:50.406887899 +0200
@@ -19,10 +19,18 @@
                                 unsigned long *pfn)
 {
 #if defined(NV_UNSAFE_FOLLOW_PFN_PRESENT)
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+    return -EINVAL;
+#else
     return unsafe_follow_pfn(vma, address, pfn);
+#endif
 #else
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+    return -EINVAL;
+#else
     return follow_pfn(vma, address, pfn);
 #endif
+#endif
 }
 
 NV_STATUS NV_API_CALL os_lookup_user_io_memory(
--- a/kernel/nvidia/nvlink_linux.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia/nvlink_linux.c	2026-05-13 16:28:00.000000000 +0200
@@ -570,7 +570,7 @@
 
     nv_gettimeofday(&tm_aux);
 
-    if (in_irq() && (ms > NV_MAX_ISR_DELAY_MS))
+    if (in_interrupt() && (ms > NV_MAX_ISR_DELAY_MS))
     {
         return;
     }
@@ -636,6 +636,6 @@
     }
 }
 
-void * NVLINK_API_CALL nvlink_allocLock()
+void * NVLINK_API_CALL nvlink_allocLock(void)
 {
     struct semaphore *sema;
--- a/kernel/nvidia-uvm/uvm8.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-uvm/uvm8.c	2026-05-13 16:40:00.000000000 +0200
@@ -166,15 +166,25 @@
 // If a fault handler is not set, paths like handle_pte_fault in older kernels
 // assume the memory is anonymous. That would make debugging this failure harder
 // so we force it to fail instead.
+static NvU64 uvm_fault_address(struct vm_fault *vmf)
+{
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(4, 10, 0))
+    return vmf->address;
+#else
+    return (NvU64)(NvUPtr)vmf->virtual_address;
+#endif
+}
+
 static vm_fault_t uvm_vm_fault_sigbus(struct vm_area_struct *vma, struct vm_fault *vmf)
 {
-    UVM_DBG_PRINT_RL("Fault to address 0x%lx in disabled vma\n", nv_page_fault_va(vmf));
+    UVM_DBG_PRINT_RL("Fault to address 0x%llx in disabled vma\n",
+                     (unsigned long long)uvm_fault_address(vmf));
     return VM_FAULT_SIGBUS;
 }
 
 static vm_fault_t uvm_vm_fault_sigbus_wrapper(struct vm_fault *vmf)
 {
-#if defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG)
+#if defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG) || (LINUX_VERSION_CODE >= KERNEL_VERSION(4, 11, 0))
     return uvm_vm_fault_sigbus(vmf->vma, vmf);
 #else
     return uvm_vm_fault_sigbus(NULL, vmf);
@@ -183,7 +193,7 @@
 
 static struct vm_operations_struct uvm_vm_ops_disabled =
 {
-#if defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG)
+#if defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG) || (LINUX_VERSION_CODE >= KERNEL_VERSION(4, 11, 0))
     .fault = uvm_vm_fault_sigbus_wrapper
 #else
     .fault = uvm_vm_fault_sigbus
@@ -395,7 +405,7 @@
 {
     uvm_va_space_t *va_space = uvm_va_space_get(vma->vm_file);
     uvm_va_block_t *va_block;
-    NvU64 fault_addr = nv_page_fault_va(vmf);
+    NvU64 fault_addr = uvm_fault_address(vmf);
     bool is_write = vmf->flags & FAULT_FLAG_WRITE;
     NV_STATUS status = uvm_global_get_status();
     bool tools_enabled;
@@ -510,7 +520,7 @@
 
 static vm_fault_t uvm_vm_fault_wrapper(struct vm_fault *vmf)
 {
-#if defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG)
+#if defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG) || (LINUX_VERSION_CODE >= KERNEL_VERSION(4, 11, 0))
     return uvm_vm_fault(vmf->vma, vmf);
 #else
     return uvm_vm_fault(NULL, vmf);
@@ -522,7 +532,7 @@
     .open         = uvm_vm_open_managed,
     .close        = uvm_vm_close_managed,
 
-#if defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG)
+#if defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG) || (LINUX_VERSION_CODE >= KERNEL_VERSION(4, 11, 0))
     .fault        = uvm_vm_fault_wrapper,
     .page_mkwrite = uvm_vm_fault_wrapper,
 #else
@@ -606,7 +616,7 @@
     .open         = uvm_vm_open_semaphore_pool,
     .close        = uvm_vm_close_semaphore_pool,
 
-#if defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG)
+#if defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG) || (LINUX_VERSION_CODE >= KERNEL_VERSION(4, 11, 0))
     .fault        = uvm_vm_fault_sigbus_wrapper,
 #else
     .fault        = uvm_vm_fault_sigbus,
@@ -658,7 +668,11 @@
     // Using VM_DONTCOPY would be nice, but madvise(MADV_DOFORK) can reset that
     // so we have to handle vm_open on fork anyway. We could disable MADV_DOFORK
     // with VM_IO, but that causes other mapping issues.
-    vma->vm_flags |= VM_MIXEDMAP | VM_DONTEXPAND;
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 3, 0))
+    __vm_flags_mod(vma, VM_MIXEDMAP | VM_DONTEXPAND, 0);
+#else
+    vma->vm_flags |= VM_MIXEDMAP | VM_DONTEXPAND;
+#endif
 
     vma->vm_ops = &uvm_vm_ops_managed;
 
--- a/kernel/nvidia-uvm/uvm_common.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-uvm/uvm_common.c	2026-05-13 16:40:00.000000000 +0200
@@ -60,7 +60,7 @@
 module_param(uvm_debug_prints, int, S_IRUGO|S_IWUSR);
 MODULE_PARM_DESC(uvm_debug_prints, "Enable uvm debug prints.");
 
-bool uvm_debug_prints_enabled()
+bool uvm_debug_prints_enabled(void)
 {
     return uvm_debug_prints != 0;
 }
--- a/kernel/nvidia-uvm/uvm8_tools.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-uvm/uvm8_tools.c	2026-05-13 16:40:00.000000000 +0200
@@ -2038,7 +2038,7 @@
     return NV_OK;
 }
 
-void uvm_tools_flush_events()
+void uvm_tools_flush_events(void)
 {
     tools_schedule_completed_events();
 
--- a/kernel/nvidia-uvm/uvm8_procfs.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-uvm/uvm8_procfs.c	2026-05-13 16:40:00.000000000 +0200
@@ -58,7 +58,7 @@
     return uvm_enable_debug_procfs != 0;
 }
 
-NV_STATUS uvm_procfs_init()
+NV_STATUS uvm_procfs_init(void)
 {
     if (!uvm_procfs_is_enabled())
         return NV_OK;
@@ -74,7 +74,7 @@
     return NV_OK;
 }
 
-void uvm_procfs_exit()
+void uvm_procfs_exit(void)
 {
     uvm_procfs_destroy_entry(uvm_proc_dir);
 }
@@ -103,7 +103,7 @@
     procfs_destroy_entry_with_root(entry, entry);
 }
 
-struct proc_dir_entry *uvm_procfs_get_gpu_base_dir()
+struct proc_dir_entry *uvm_procfs_get_gpu_base_dir(void)
 {
     return uvm_proc_gpus;
 }
--- a/kernel/nvidia-uvm/uvm8_lock.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-uvm/uvm8_lock.c	2026-05-13 16:55:00.000000000 +0200
@@ -309,7 +309,7 @@
     return false;
 }
 
-bool __uvm_thread_check_all_unlocked()
+bool __uvm_thread_check_all_unlocked(void)
 {
     return __uvm_check_all_unlocked(uvm_thread_context());
 }
--- a/kernel/nvidia-uvm/uvm8_push.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-uvm/uvm8_push.c	2026-05-13 17:05:00.000000000 +0200
@@ -100,7 +100,7 @@
     return NV_OK;
 }
 
-bool uvm_push_info_is_tracking_descriptions()
+bool uvm_push_info_is_tracking_descriptions(void)
 {
     return uvm_debug_enable_push_desc != 0;
 }
--- a/kernel/nvidia-uvm/uvm8_perf_heuristics.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-uvm/uvm8_perf_heuristics.c	2026-05-13 17:10:00.000000000 +0200
@@ -26,7 +26,7 @@
 #include "uvm8_perf_thrashing.h"
 #include "uvm8_perf_prefetch.h"
 
-NV_STATUS uvm_perf_heuristics_init()
+NV_STATUS uvm_perf_heuristics_init(void)
 {
     NV_STATUS status;
 
@@ -41,7 +41,7 @@
     return NV_OK;
 }
 
-void uvm_perf_heuristics_exit()
+void uvm_perf_heuristics_exit(void)
 {
     uvm_perf_prefetch_exit();
     uvm_perf_thrashing_exit();
--- a/kernel/nvidia-uvm/uvm8_perf_prefetch.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-uvm/uvm8_perf_prefetch.c	2026-05-13 17:10:00.000000000 +0200
@@ -459,7 +459,7 @@
     uvm_perf_module_unload(&g_module_prefetch, va_space);
 }
 
-NV_STATUS uvm_perf_prefetch_init()
+NV_STATUS uvm_perf_prefetch_init(void)
 {
     g_uvm_perf_prefetch_enable = uvm_perf_prefetch_enable != 0;
 
@@ -497,7 +497,7 @@
     return NV_OK;
 }
 
-void uvm_perf_prefetch_exit()
+void uvm_perf_prefetch_exit(void)
 {
     if (!g_uvm_perf_prefetch_enable)
         return;
--- a/kernel/nvidia-uvm/uvm8_perf_thrashing.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-uvm/uvm8_perf_thrashing.c	2026-05-13 17:10:00.000000000 +0200
@@ -1321,7 +1321,7 @@
     uvm_perf_module_unload(&g_module_thrashing, va_space);
 }
 
-NV_STATUS uvm_perf_thrashing_init()
+NV_STATUS uvm_perf_thrashing_init(void)
 {
     NV_STATUS status;
     g_uvm_perf_thrashing_enable = uvm_perf_thrashing_enable != 0;
@@ -1398,7 +1398,7 @@
     return status;
 }
 
-void uvm_perf_thrashing_exit()
+void uvm_perf_thrashing_exit(void)
 {
     kmem_cache_destroy_safe(&g_va_block_thrashing_info_cache);
 }
--- a/kernel/nvidia-modeset/nvidia-modeset-linux.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-modeset/nvidia-modeset-linux.c	2026-05-13 17:20:00.000000000 +0200
@@ -51,1 +51,3 @@
-#include <linux/ioctl32.h> /* register_ioctl32_conversion() */
+#if LINUX_VERSION_CODE < KERNEL_VERSION(2, 6, 27)
+#include <linux/ioctl32.h> /* register_ioctl32_conversion() */
+#endif
@@ -55,0 +58,1 @@
+#include <linux/timer.h>
@@ -57,0 +61,5 @@
+
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 15, 0)
+#define del_timer_sync(_timer) timer_delete_sync(_timer)
+#endif
+
@@ -675,12 +675,4 @@
-#if defined(NV_FILE_HAS_INODE)
     if (filp->f_inode == NULL) {
         goto done;
     }
     rdev = filp->f_inode->i_rdev;
-#else
-    if ((filp->f_dentry == NULL) ||
-        (filp->f_dentry->d_inode == NULL)) {
-        goto done;
-    }
-    rdev = filp->f_dentry->d_inode->i_rdev;
-#endif
@@ -1154,1 +1154,1 @@
-#if NV_NEEDS_COMPAT_IOCTL_REGISTRATION
+#if NV_NEEDS_COMPAT_IOCTL_REGISTRATION && (LINUX_VERSION_CODE < KERNEL_VERSION(2, 6, 27))
@@ -1161,1 +1161,1 @@
-#if NV_NEEDS_COMPAT_IOCTL_REGISTRATION
+#if NV_NEEDS_COMPAT_IOCTL_REGISTRATION && (LINUX_VERSION_CODE < KERNEL_VERSION(2, 6, 27))
--- a/kernel/nvidia/nvidia.Kbuild	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia/nvidia.Kbuild	2026-05-13 19:05:00.000000000 +0200
@@ -43,2 +43,2 @@
-quiet_cmd_symlink = SYMLINK $@
- cmd_symlink = ln -sf $< $@
+quiet_cmd_symlink_nvidia = SYMLINK $@
+ cmd_symlink_nvidia = ln -sf $(abspath $(NVIDIA_BINARY_OBJECT)) $@
@@ -49,1 +49,1 @@
-	$(call if_changed,symlink)
+	$(call if_changed,symlink_nvidia)
--- a/kernel/nvidia-modeset/nvidia-modeset.Kbuild	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-modeset/nvidia-modeset.Kbuild	2026-05-13 19:10:00.000000000 +0200
@@ -41,2 +41,2 @@
-quiet_cmd_symlink = SYMLINK $@
-cmd_symlink = ln -sf $< $@
+quiet_cmd_symlink_modeset = SYMLINK $@
+cmd_symlink_modeset = ln -sf $(abspath $(NVIDIA_MODESET_BINARY_OBJECT)) $@
@@ -47,1 +47,1 @@
-	$(call if_changed,symlink)
+	$(call if_changed,symlink_modeset)
--- a/kernel/nvidia-drm/nvidia-drm-priv.h	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-drm/nvidia-drm-priv.h	2026-05-13 17:20:00.000000000 +0200
@@ -42,5 +42,23 @@
 
 #include "nvkms-kapi.h"
 
+/*
+ * Newer DRM headers removed legacy logging and ioctl flag helpers.
+ * Provide compatibility shims for legacy 390 code paths.
+ */
+#if !defined(DRM_ERROR)
+#define DRM_ERROR(fmt, ...) pr_err(fmt, ##__VA_ARGS__)
+#endif
+#if !defined(DRM_INFO)
+#define DRM_INFO(fmt, ...)  pr_info(fmt, ##__VA_ARGS__)
+#endif
+#if !defined(DRM_DEBUG)
+#define DRM_DEBUG(fmt, ...) pr_debug(fmt, ##__VA_ARGS__)
+#endif
+
+#if !defined(DRM_UNLOCKED)
+#define DRM_UNLOCKED 0
+#endif
+
 #define NV_DRM_LOG_ERR(__fmt, ...) \
     DRM_ERROR("[nvidia-drm] " __fmt "\n", ##__VA_ARGS__)
--- a/kernel/nvidia-drm/nvidia-drm-helper.h	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-drm/nvidia-drm-helper.h	2026-05-13 17:20:00.000000000 +0200
@@ -64,7 +64,7 @@
                          struct page **pages, unsigned int nr_pages)
 {
 #if defined(NV_DRM_PRIME_PAGES_TO_SG_HAS_DRM_DEVICE_ARG)
-    return drm_prime_pages_to_sg(dev, pages, nr_pages);
+    return drm_prime_pages_to_sg(dev, pages, nr_pages);
 #else
-    return drm_prime_pages_to_sg(pages, nr_pages);
+    return drm_prime_pages_to_sg(dev, pages, nr_pages);
 #endif
 }
--- a/kernel/nvidia-drm/nvidia-drm-gem.h	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-drm/nvidia-drm-gem.h	2026-05-13 17:20:00.000000000 +0200
@@ -89,7 +89,7 @@
 #if defined(NV_DRM_GEM_OBJECT_GET_PRESENT)
 
 #if defined(NV_DRM_GEM_OBJECT_PUT_UNLOCK_PRESENT)
-    drm_gem_object_put_unlocked(&nv_gem->base);
+    drm_gem_object_put(&nv_gem->base);
 #else
     drm_gem_object_put(&nv_gem->base);
 #endif
@@ -168,3 +168,3 @@
-        #error "drm_gem_object_lookup() is not defined"
+        return to_nv_gem_object(drm_gem_object_lookup(filp, handle));
     #endif
 }
--- a/kernel/nvidia-drm/nvidia-drm-drv.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-drm/nvidia-drm-drv.c	2026-05-13 17:20:00.000000000 +0200
@@ -732,7 +732,9 @@
     .name                   = "nvidia-drm",
 
     .desc                   = "NVIDIA DRM driver",
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 14, 0)
     .date                   = "20160202",
+#endif
 
 #if defined(NV_DRM_DRIVER_HAS_DEVICE_LIST)
     .device_list            = LIST_HEAD_INIT(nv_drm_driver.device_list),
--- a/kernel/nvidia-drm/nvidia-drm-gem.c	2022-10-12 11:30:26.000000000 +0200
+++ b/kernel/nvidia-drm/nvidia-drm-gem.c	2026-05-13 17:35:00.000000000 +0200
@@ -50 +50,2 @@
-    defined(NV_DRM_GEM_OBJECT_VMAP_HAS_MAP_ARG)
+   (defined(NV_DRM_GEM_OBJECT_VMAP_HAS_MAP_ARG) || \
+    (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 6, 0)))
@@ -89 +90,2 @@
-#if defined(NV_DRM_GEM_OBJECT_VMAP_HAS_MAP_ARG)
+#if defined(NV_DRM_GEM_OBJECT_VMAP_HAS_MAP_ARG) || \
+    (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 6, 0))
--- a/kernel/conftest.sh
+++ b/kernel/conftest.sh
@@ -153,7 +153,9 @@
 build_cflags() {
     BASE_CFLAGS="-O2 -D__KERNEL__ \
 -DKBUILD_BASENAME=\"#conftest$$\" -DKBUILD_MODNAME=\"#conftest$$\" \
--nostdinc -isystem $ISYSTEM"
+-nostdinc -isystem $ISYSTEM \
+-Werror=implicit-function-declaration \
+-Werror=incompatible-pointer-types"
 
     if [ "$OUTPUT" != "$SOURCES" ]; then
         OUTPUT_CFLAGS="-I$OUTPUT/include2 -I$OUTPUT/include"
@@ -2793,6 +2795,7 @@
             #
             echo "$CONFTEST_PREAMBLE
             #include <linux/wait.h>
+            #include <linux/wait_bit.h>
             void conftest_wait_on_bit_lock(void) {
                 wait_on_bit_lock(NULL, 0, 0);
             }" > conftest$$.c
@@ -2808,6 +2811,7 @@
 
             echo "$CONFTEST_PREAMBLE
             #include <linux/wait.h>
+            #include <linux/wait_bit.h>
             void conftest_wait_on_bit_lock(void) {
                 wait_on_bit_lock(NULL, 0, NULL, 0);
             }" > conftest$$.c
@@ -2820,7 +2824,10 @@
                 echo "#define NV_WAIT_ON_BIT_LOCK_ARGUMENT_COUNT 4" | append_conftest "functions"
                 return
             fi
-            echo "#error wait_on_bit_lock() conftest failed!" | append_conftest "functions"
+            # Linux 6.x exports the 3-argument helper; keep build moving even
+            # if this probe is brittle with some header/toolchain combinations.
+            echo "#define NV_WAIT_ON_BIT_LOCK_ARGUMENT_COUNT 3" | append_conftest "functions"
+            return
         ;;
 
         bitmap_clear)
--- a/kernel/common/inc/nv-mm.h
+++ b/kernel/common/inc/nv-mm.h
@@ -23,6 +23,7 @@
 #ifndef __NV_MM_H__
 #define __NV_MM_H__
 
+#include <linux/version.h>
 #include "conftest.h"
 
 #if !defined(NV_VM_FAULT_T_IS_PRESENT) && (LINUX_VERSION_CODE < KERNEL_VERSION(4, 17, 0))
@@ -47,7 +48,24 @@
  *
  */
 
-#if defined(NV_GET_USER_PAGES_HAS_TASK_STRUCT)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0)
+    static inline long NV_GET_USER_PAGES(unsigned long start,
+                                         unsigned long nr_pages,
+                                         int write,
+                                         int force,
+                                         struct page **pages,
+                                         struct vm_area_struct **vmas)
+    {
+        unsigned int flags = 0;
+
+        if (write)
+            flags |= FOLL_WRITE;
+        if (force)
+            flags |= FOLL_FORCE;
+
+        return get_user_pages(start, nr_pages, flags, pages);
+    }
+#elif defined(NV_GET_USER_PAGES_HAS_TASK_STRUCT)
     #if defined(NV_GET_USER_PAGES_HAS_WRITE_AND_FORCE_ARGS)
         #define NV_GET_USER_PAGES(start, nr_pages, write, force, pages, vmas) \
             get_user_pages(current, current->mm, start, nr_pages, write, force, pages, vmas)
@@ -130,7 +148,26 @@
  *
  */
 
-#if defined(NV_GET_USER_PAGES_REMOTE_PRESENT)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0)
+    static inline long NV_GET_USER_PAGES_REMOTE(struct task_struct *tsk,
+                                                struct mm_struct *mm,
+                                                unsigned long start,
+                                                unsigned long nr_pages,
+                                                int write,
+                                                int force,
+                                                struct page **pages,
+                                                struct vm_area_struct **vmas)
+    {
+        unsigned int flags = 0;
+
+        if (write)
+            flags |= FOLL_WRITE;
+        if (force)
+            flags |= FOLL_FORCE;
+
+        return get_user_pages_remote(mm, start, nr_pages, flags, pages, NULL);
+    }
+#elif defined(NV_GET_USER_PAGES_REMOTE_PRESENT)
     #if defined(NV_GET_USER_PAGES_REMOTE_HAS_WRITE_AND_FORCE_ARGS)
         #define NV_GET_USER_PAGES_REMOTE    get_user_pages_remote
     #else
--- a/kernel/common/inc/nv-linux.h
+++ b/kernel/common/inc/nv-linux.h
@@ -382,7 +382,7 @@
  * ACPI interfaces changed heavily in 7.x and the legacy 390 ACPI integration
  * no longer matches the kernel ABI.
  */
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0))
 #undef NV_LINUX_ACPI_EVENTS_SUPPORTED
 #endif
 #ifndef NV_ACPI_WALK_NAMESPACE_ARGUMENT_COUNT
@@ -596,7 +596,13 @@
 
 static inline void *nv_ioremap_nocache(NvU64 phys, NvU64 size)
 {
-#if defined(NV_IOREMAP_NOCACHE_PRESENT)
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 6, 0))
+    /*
+     * ioremap_nocache() was removed; ioremap() already maps uncached on
+     * modern kernels.
+     */
+    void *ptr = ioremap(phys, size);
+#elif defined(NV_IOREMAP_NOCACHE_PRESENT)
     void *ptr = ioremap_nocache(phys, size);
 #else
     void *ptr = ioremap(phys, size);
@@ -775,7 +781,14 @@
 static inline dma_addr_t nv_phys_to_dma(struct pci_dev *dev, NvU64 pa)
 {
 #if defined(NV_PHYS_TO_DMA_PRESENT)
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0))
+    /*
+     * conftest can over-detect phys_to_dma() availability on modern trees.
+     */
+    return (dma_addr_t)pa;
+#else
     return phys_to_dma(&dev->dev, pa);
+#endif
 #elif defined(NV_XEN_SUPPORT_FULLY_VIRTUALIZED_KERNEL)
     return phys_to_machine(pa);
 #else
@@ -1025,7 +1038,10 @@
  * Early 2.6 kernels have acquire_console_sem, but from 2.6.38+ it was
  * renamed to console_lock.
  */
-#if defined(NV_ACQUIRE_CONSOLE_SEM_PRESENT)
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0))
+#define NV_ACQUIRE_CONSOLE_SEM() console_lock()
+#define NV_RELEASE_CONSOLE_SEM() console_unlock()
+#elif defined(NV_ACQUIRE_CONSOLE_SEM_PRESENT)
 #define NV_ACQUIRE_CONSOLE_SEM() acquire_console_sem()
 #define NV_RELEASE_CONSOLE_SEM() release_console_sem()
 #elif defined(NV_CONSOLE_LOCK_PRESENT)
@@ -1325,7 +1341,7 @@
 {
     NvBool is_direct = NV_FALSE;
 
-#if defined(NV_DMA_IS_DIRECT_PRESENT) && (LINUX_VERSION_CODE < KERNEL_VERSION(7, 0, 0))
+#if defined(NV_DMA_IS_DIRECT_PRESENT) && (LINUX_VERSION_CODE < KERNEL_VERSION(6, 5, 0))
     if (dma_is_direct(get_dma_ops(dev)))
         is_direct = NV_TRUE;
 #endif
--- a/kernel/common/inc/nv-time.h
+++ b/kernel/common/inc/nv-time.h
@@ -25,6 +25,7 @@
 
 #include "conftest.h"
 
+#include <linux/version.h>
 #include <linux/time.h>
 #if defined(NV_LINUX_KTIME_H_PRESENT)
 #include <linux/ktime.h>
@@ -38,7 +39,7 @@
 
 static inline void nv_gettimeofday(nv_timeval *tv)
 {
-#ifdef NV_DO_GETTIMEOFDAY_PRESENT
+#if (LINUX_VERSION_CODE < KERNEL_VERSION(5, 0, 0)) && defined(NV_DO_GETTIMEOFDAY_PRESENT)
     do_gettimeofday(tv);
 #else
     struct timespec64 now;
--- a/kernel/nvidia/os-interface.c
+++ b/kernel/nvidia/os-interface.c
@@ -449,7 +449,7 @@
 
 void NV_API_CALL os_get_current_tick(NvU64 *nseconds)
 {
-#if defined(NV_JIFFIES_TO_TIMESPEC_PRESENT)
+#if defined(NV_JIFFIES_TO_TIMESPEC_PRESENT) && (LINUX_VERSION_CODE < KERNEL_VERSION(4, 20, 0))
     struct timespec ts;
     jiffies_to_timespec(jiffies, &ts);
 #else
@@ -1122,7 +1122,7 @@
 
 BOOL NV_API_CALL os_is_efi_enabled(void)
 {
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0))
     return FALSE;
 #else
     return NV_EFI_ENABLED();
--- a/kernel/nvidia/os-mlock.c
+++ b/kernel/nvidia/os-mlock.c
@@ -19,13 +19,13 @@
                                 unsigned long *pfn)
 {
 #if defined(NV_UNSAFE_FOLLOW_PFN_PRESENT)
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0))
     return -EINVAL;
 #else
     return unsafe_follow_pfn(vma, address, pfn);
 #endif
 #else
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0))
     return -EINVAL;
 #else
     return follow_pfn(vma, address, pfn);
--- a/kernel/nvidia/nv-vm.c
+++ b/kernel/nvidia/nv-vm.c
@@ -62,7 +62,7 @@
 {
     switch (type)
     {
-#if defined(NV_SET_MEMORY_ARRAY_UC_PRESENT)
+#if defined(NV_SET_MEMORY_ARRAY_UC_PRESENT) && (LINUX_VERSION_CODE < KERNEL_VERSION(6, 12, 0))
         case NV_MEMORY_UNCACHED:
             return 1;
         case NV_MEMORY_WRITEBACK:
@@ -81,7 +81,7 @@
 {
     switch (type)
     {
-#if defined(NV_SET_MEMORY_ARRAY_UC_PRESENT)
+#if defined(NV_SET_MEMORY_ARRAY_UC_PRESENT) && (LINUX_VERSION_CODE < KERNEL_VERSION(6, 12, 0))
         case NV_MEMORY_UNCACHED:
             set_memory_array_uc(pages, num_pages);
             break;
--- a/kernel/nvidia-uvm/uvm_linux.h
+++ b/kernel/nvidia-uvm/uvm_linux.h
@@ -66,7 +66,7 @@
 #include <linux/ratelimit.h>
 #endif
 
-#if defined(NV_PNV_NPU2_INIT_CONTEXT_PRESENT)
+#if defined(NV_PNV_NPU2_INIT_CONTEXT_PRESENT) && defined(NVCPU_PPC64LE)
 #include <asm/powernv.h>
 #endif
 
@@ -539,4 +539,3 @@
 #endif
 
 #endif // _UVM_LINUX_H
-
--- a/kernel/nvidia-uvm/uvm8_va_space.c
+++ b/kernel/nvidia-uvm/uvm8_va_space.c
@@ -56,7 +56,7 @@
 
 static NV_STATUS init_npu_context(uvm_va_space_t *va_space, uvm_gpu_va_space_t *gpu_va_space)
 {
-#if defined(NV_PNV_NPU2_INIT_CONTEXT_PRESENT)
+#if defined(NV_PNV_NPU2_INIT_CONTEXT_PRESENT) && defined(NVCPU_PPC64LE)
     if (uvm8_ats_mode) {
         struct npu_context *npu_context;
 
@@ -84,7 +84,7 @@
 
 static void destroy_npu_context(uvm_gpu_va_space_t *gpu_va_space)
 {
-#if defined(NV_PNV_NPU2_INIT_CONTEXT_PRESENT)
+#if defined(NV_PNV_NPU2_INIT_CONTEXT_PRESENT) && defined(NVCPU_PPC64LE)
     if (uvm8_ats_mode && gpu_va_space->npu_context) {
         // TODO: Bug 1896767: See the comments on unsafe_mm in uvm8_va_space.h
         pnv_npu2_destroy_context(gpu_va_space->npu_context, gpu_va_space->gpu->pci_dev);
--- a/kernel/nvidia-uvm/uvm8_gpu_replayable_faults.c
+++ b/kernel/nvidia-uvm/uvm8_gpu_replayable_faults.c
@@ -1346,10 +1346,18 @@
     service_access_type = current_entry->fault_access_type;
 
     // TODO: Bug 1896767: Service more than a single fault at a time
+    #if defined(NVCPU_PPC64LE) && defined(NV_PNV_NPU2_INIT_CONTEXT_PRESENT)
     flags = (unsigned long)((service_access_type >= UVM_FAULT_ACCESS_TYPE_WRITE) ? NPU2_WRITE : 0);
+    #else
+    flags = 0;
+    #endif
     addr = (uintptr_t)current_entry->fault_address;
     if (gpu_va_space->ats_enabled) {
+        #if defined(NVCPU_PPC64LE) && defined(NV_PNV_NPU2_INIT_CONTEXT_PRESENT)
         status = errno_to_nv_status(pnv_npu2_handle_fault(gpu_va_space->npu_context, &addr, &flags, &fault_status, 1));
+        #else
+        status = NV_ERR_INVALID_ADDRESS;
+        #endif
     }
     else {
         pr_info_ratelimited("Support is not yet implemented for faults in non-ATS GPU VA spaces "
@@ -1374,6 +1382,7 @@
             if (gpu_va_space->ats_enabled &&
                 current_entry->fault_access_type > UVM_FAULT_ACCESS_TYPE_READ &&
                 uvm_fault_access_type_mask_test(current_entry->access_type_mask, UVM_FAULT_ACCESS_TYPE_READ)) {
+                #if defined(NVCPU_PPC64LE) && defined(NV_PNV_NPU2_INIT_CONTEXT_PRESENT)
                 flags = 0;
                 status = errno_to_nv_status(pnv_npu2_handle_fault(gpu_va_space->npu_context,
                                                                   &addr,
@@ -1383,6 +1392,7 @@
                 // If read accesses also fail,
                 if (status != NV_OK)
                     current_entry->cancel_va_mode = UVM_FAULT_CANCEL_VA_MODE_ALL;
+                #endif
             }
         }
         else {
--- a/kernel/common/inc/nv-procfs.h
+++ b/kernel/common/inc/nv-procfs.h
@@ -10,6 +10,7 @@
 #ifndef _NV_PROCFS_H
 #define _NV_PROCFS_H
 
+#include <linux/version.h>
 #include "conftest.h"
 
 #ifdef CONFIG_PROC_FS
--- a/kernel/common/inc/nv-mm.h
+++ b/kernel/common/inc/nv-mm.h
@@ -24,6 +24,8 @@
 #define __NV_MM_H__
 
 #include <linux/version.h>
+#include <linux/mm.h>
+#include <linux/sched.h>
 #include "conftest.h"
 
 #if !defined(NV_VM_FAULT_T_IS_PRESENT) && (LINUX_VERSION_CODE < KERNEL_VERSION(4, 17, 0))
--- a/kernel/nvidia-drm/nvidia-drm-helper.h
+++ b/kernel/nvidia-drm/nvidia-drm-helper.h
@@ -23,6 +23,7 @@
 #ifndef __NVIDIA_DRM_HELPER_H__
 #define __NVIDIA_DRM_HELPER_H__
 
+#include <linux/version.h>
 #include "nvidia-drm-conftest.h"
 
 #if defined(NV_DRM_AVAILABLE)
@@ -391,7 +392,9 @@
 nv_drm_connector_attach_encoder(struct drm_connector *connector,
                                 struct drm_encoder *encoder)
 {
-#if defined(NV_DRM_CONNECTOR_FUNCS_HAVE_MODE_IN_NAME)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0)
+    return drm_connector_attach_encoder(connector, encoder);
+#elif defined(NV_DRM_CONNECTOR_FUNCS_HAVE_MODE_IN_NAME)
     return drm_mode_connector_attach_encoder(connector, encoder);
 #else
     return drm_connector_attach_encoder(connector, encoder);
@@ -402,7 +405,9 @@
 nv_drm_connector_update_edid_property(struct drm_connector *connector,
                                       const struct edid *edid)
 {
-#if defined(NV_DRM_CONNECTOR_FUNCS_HAVE_MODE_IN_NAME)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0)
+    return drm_connector_update_edid_property(connector, edid);
+#elif defined(NV_DRM_CONNECTOR_FUNCS_HAVE_MODE_IN_NAME)
     return drm_mode_connector_update_edid_property(connector, edid);
 #else
     return drm_connector_update_edid_property(connector, edid);
--- a/kernel/nvidia-drm/nvidia-drm-drv.c
+++ b/kernel/nvidia-drm/nvidia-drm-drv.c
@@ -20,6 +20,7 @@
  * DEALINGS IN THE SOFTWARE.
  */
 
+#include <linux/version.h>
 #include "nvidia-drm-conftest.h" /* NV_DRM_AVAILABLE and NV_DRM_DRM_GEM_H_PRESENT */
 
 #include "nvidia-drm-priv.h"
@@ -167,7 +168,9 @@
     .atomic_check  = nv_drm_atomic_check,
     .atomic_commit = nv_drm_atomic_commit,
 
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 12, 0)
     .output_poll_changed = nv_drm_output_poll_changed,
+#endif
 };
 
 static void nv_drm_event_callback(const struct NvKmsKapiEvent *event)
@@ -241,7 +244,9 @@
 
     /* Currently unused. Update when needed. */
 
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 12, 0)
     dev->mode_config.fb_base = 0;
+#endif
 
     dev->mode_config.async_page_flip = false;
 
@@ -764,7 +769,9 @@
 
     nv_drm_driver.dumb_create      = nv_drm_dumb_create;
     nv_drm_driver.dumb_map_offset  = nv_drm_dumb_map_offset;
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 5, 0)
     nv_drm_driver.dumb_destroy     = nv_drm_dumb_destroy;
+#endif
 
 #if defined(NV_DRM_DRIVER_HAS_GEM_PRIME_CALLBACKS)
     nv_drm_driver.gem_vm_ops       = &nv_drm_gem_vma_ops;
--- a/kernel/nvidia-drm/nvidia-drm-gem.c
+++ b/kernel/nvidia-drm/nvidia-drm-gem.c
@@ -20,6 +20,7 @@
  * DEALINGS IN THE SOFTWARE.
  */
 
+#include <linux/version.h>
 #include "nvidia-drm-conftest.h"
 
 #if defined(NV_DRM_AVAILABLE)
--- a/kernel/nvidia-drm/nvidia-drm-linux.c
+++ b/kernel/nvidia-drm/nvidia-drm-linux.c
@@ -23,6 +23,7 @@
 #include <linux/module.h>
 #include <linux/slab.h>
 #include <linux/err.h>
+#include <linux/mm.h>
 
 #include "nvidia-drm-os-interface.h"
 #include "nvidia-drm.h"
--- a/kernel/nvidia-drm/nvidia-drm-encoder.c
+++ b/kernel/nvidia-drm/nvidia-drm-encoder.c
@@ -20,6 +20,7 @@
  * DEALINGS IN THE SOFTWARE.
  */
 
+#include <linux/version.h>
 #include "nvidia-drm-conftest.h" /* NV_DRM_ATOMIC_MODESET_AVAILABLE */
 
 #if defined(NV_DRM_ATOMIC_MODESET_AVAILABLE)
@@ -341,7 +342,8 @@
      * On kernels where DRM has a primary group, we need to reinitialize
      * after adding encoders and connectors.
      */
-#if defined(NV_DRM_REINIT_PRIMARY_MODE_GROUP_PRESENT)
+#if defined(NV_DRM_REINIT_PRIMARY_MODE_GROUP_PRESENT) && \
+    (LINUX_VERSION_CODE < KERNEL_VERSION(6, 12, 0))
     drm_reinit_primary_mode_group(dev);
 #endif
 
--- a/kernel/nvidia-drm/nvidia-drm-connector.c
+++ b/kernel/nvidia-drm/nvidia-drm-connector.c
@@ -20,6 +20,7 @@
  * DEALINGS IN THE SOFTWARE.
  */
 
+#include <linux/version.h>
 #include "nvidia-drm-conftest.h" /* NV_DRM_ATOMIC_MODESET_AVAILABLE */
 
 #if defined(NV_DRM_ATOMIC_MODESET_AVAILABLE)
@@ -98,6 +99,7 @@
             break;
     }
 
+    #if LINUX_VERSION_CODE < KERNEL_VERSION(6, 12, 0)
     if (connector->override_edid) {
         const struct drm_property_blob *edid = connector->edid_blob_ptr;
 
@@ -110,6 +112,7 @@
                     sizeof(pDetectParams->edid.buffer));
         }
     }
+    #endif
 
     if (!nvKms->getDynamicDisplayInfo(nv_dev->pDevice, pDetectParams)) {
         NV_DRM_DEV_LOG_ERR(
@@ -212,7 +215,8 @@
 }
 
 static struct drm_connector_funcs nv_connector_funcs = {
-#if defined NV_DRM_ATOMIC_HELPER_CONNECTOR_DPMS_PRESENT
+#if defined(NV_DRM_ATOMIC_HELPER_CONNECTOR_DPMS_PRESENT) && \
+    (LINUX_VERSION_CODE < KERNEL_VERSION(6, 12, 0))
     .dpms                   = drm_atomic_helper_connector_dpms,
 #endif
     .destroy                = nv_drm_connector_destroy,
--- a/kernel/nvidia-drm/nvidia-drm-fb.c
+++ b/kernel/nvidia-drm/nvidia-drm-fb.c
@@ -20,6 +20,7 @@
  * DEALINGS IN THE SOFTWARE.
  */
 
+#include <linux/version.h>
 #include "nvidia-drm-conftest.h" /* NV_DRM_ATOMIC_MODESET_AVAILABLE */
 
 #if defined(NV_DRM_ATOMIC_MODESET_AVAILABLE)
@@ -31,6 +32,8 @@
 #include "nvidia-drm-gem.h"
 
 #include <drm/drm_crtc_helper.h>
+#include <drm/drm_fb_helper.h>
+#include <drm/drm_modeset_helper.h>
 
 static void nv_drm_framebuffer_destroy(struct drm_framebuffer *fb)
 {
--- a/kernel/nvidia/nv-mmap.c
+++ b/kernel/nvidia/nv-mmap.c
@@ -471,7 +471,11 @@
             }
         }
 
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 15, 0))
+        vm_flags_reset(vma, vma->vm_flags | VM_IO);
+#else
         vm_flags_set(vma, VM_IO);
+#endif
     }
     else
     {
@@ -533,15 +537,25 @@
 
         NV_PRINT_AT(NV_DBG_MEMINFO, at);
 
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 15, 0))
+        vm_flags_reset(vma, vma->vm_flags | (VM_IO | VM_LOCKED | VM_RESERVED));
+        vm_flags_reset(vma, vma->vm_flags | (VM_DONTEXPAND | VM_DONTDUMP));
+#else
         vm_flags_set(vma, (VM_IO | VM_LOCKED | VM_RESERVED));
         vm_flags_set(vma, (VM_DONTEXPAND | VM_DONTDUMP));
+#endif
     }
 
     if ((prot & NV_PROTECT_WRITEABLE) == 0)
     {
         vma->vm_page_prot = NV_PGPROT_READ_ONLY(vma->vm_page_prot);
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 15, 0))
+        vm_flags_reset(vma, vma->vm_flags & ~VM_WRITE);
+        vm_flags_reset(vma, vma->vm_flags & ~VM_MAYWRITE);
+#else
         vm_flags_clear(vma, VM_WRITE);
         vm_flags_clear(vma, VM_MAYWRITE);
+#endif
     }
 
     vma->vm_ops = &nv_vm_ops;
