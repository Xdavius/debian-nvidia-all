diff --git a/kernel-dkms/Kbuild b/kernel-dkms/Kbuild
--- a/kernel-dkms/Kbuild
+++ b/kernel-dkms/Kbuild
@@ -73,6 +73,10 @@ EXTRA_CFLAGS += $(call cc-option,-Werror=undef,)
 EXTRA_CFLAGS += -DNV_SPECTRE_V2=$(NV_SPECTRE_V2)
 EXTRA_CFLAGS += -DNV_KERNEL_INTERFACE_LAYER
 
+# Compatibility with newer kernels/toolchains where relying only on
+# EXTRA_CFLAGS may miss local include dirs during module compilation.
+ccflags-y += $(EXTRA_CFLAGS)
+
 #
 # Detect SGI UV systems and apply system-specific optimizations.
 #
diff --git a/kernel-dkms/Kbuild b/kernel-dkms/Kbuild
--- a/kernel-dkms/Kbuild
+++ b/kernel-dkms/Kbuild
@@ -68,7 +68,7 @@ $(foreach _module, $(NV_KERNEL_MODULES), 
 
 EXTRA_CFLAGS += -I$(src)/common/inc
 EXTRA_CFLAGS += -I$(src)
-EXTRA_CFLAGS += -Wall -MD $(DEFINES) $(INCLUDES) -Wno-cast-qual -Wno-error -Wno-format-extra-args
+EXTRA_CFLAGS += -Wall $(DEFINES) $(INCLUDES) -Wno-cast-qual -Wno-error -Wno-format-extra-args
 EXTRA_CFLAGS += -D__KERNEL__ -DMODULE -DNVRM -DNV_VERSION_STRING="470.256.02" -Wno-unused-function -Wuninitialized -fno-strict-aliasing -mno-red-zone -mcmodel=kernel -DNV_UVM_ENABLE
 EXTRA_CFLAGS += $(call cc-option,-Werror=undef,)
 EXTRA_CFLAGS += -DNV_SPECTRE_V2=$(NV_SPECTRE_V2)
diff --git a/kernel-dkms/nvidia/nvidia.Kbuild b/kernel-dkms/nvidia/nvidia.Kbuild
--- a/kernel-dkms/nvidia/nvidia.Kbuild
+++ b/kernel-dkms/nvidia/nvidia.Kbuild
@@ -169,7 +169,7 @@ NV_CONFTEST_FUNCTION_COMPILE_TESTS += drm_gem_object_get
 NV_CONFTEST_FUNCTION_COMPILE_TESTS += drm_gem_object_put_unlocked
 NV_CONFTEST_FUNCTION_COMPILE_TESTS += set_close_on_exec
 NV_CONFTEST_FUNCTION_COMPILE_TESTS += dma_set_coherent_mask
-NV_CONFTEST_FUNCTION_COMPILE_TESTS += acpi_bus_get_device
+NV_CONFTEST_FUNCTION_COMPILE_TESTS += get_task_ioprio
 
 NV_CONFTEST_SYMBOL_COMPILE_TESTS += is_export_symbol_gpl_of_node_to_nid
 NV_CONFTEST_SYMBOL_COMPILE_TESTS += is_export_symbol_gpl_sme_active
@@ -189,7 +189,6 @@ NV_CONFTEST_SYMBOL_COMPILE_TESTS += is_export_symbol_present_dram_clk_to_mc_clk
 NV_CONFTEST_SYMBOL_COMPILE_TESTS += is_export_symbol_present_get_dram_num_channels
 NV_CONFTEST_SYMBOL_COMPILE_TESTS += is_export_symbol_present_tegra_dram_types
 
-NV_CONFTEST_TYPE_COMPILE_TESTS += acpi_op_remove
 NV_CONFTEST_TYPE_COMPILE_TESTS += file_operations
 NV_CONFTEST_TYPE_COMPILE_TESTS += file_inode
 NV_CONFTEST_TYPE_COMPILE_TESTS += kuid_t
diff --git a/kernel-dkms/common/inc/nv-mm.h b/kernel-dkms/common/inc/nv-mm.h
index a3eb362..a3ed350 100644
--- a/kernel-dkms/common/inc/nv-mm.h
+++ b/kernel-dkms/common/inc/nv-mm.h
@@ -23,8 +23,9 @@
 #ifndef __NV_MM_H__
 #define __NV_MM_H__
 
+#include <linux/version.h>
 #include "conftest.h"
 
-#if !defined(NV_VM_FAULT_T_IS_PRESENT)
+#if !defined(NV_VM_FAULT_T_IS_PRESENT) && (LINUX_VERSION_CODE < KERNEL_VERSION(6, 5, 0))
 typedef int vm_fault_t;
 #endif
 
@@ -57,7 +58,16 @@ typedef int vm_fault_t;
  *
  */
 
-#if defined(NV_GET_USER_PAGES_HAS_ARGS_FLAGS)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0)
+static inline long NV_GET_USER_PAGES(unsigned long start,
+                                     unsigned long nr_pages,
+                                     unsigned int flags,
+                                     struct page **pages,
+                                     struct vm_area_struct **vmas)
+{
+    return get_user_pages(start, nr_pages, flags, pages);
+}
+#elif defined(NV_GET_USER_PAGES_HAS_ARGS_FLAGS)
     #define NV_GET_USER_PAGES(start, nr_pages, flags, pages, vmas) \
         get_user_pages(start, nr_pages, flags, pages)
 #elif defined(NV_GET_USER_PAGES_HAS_ARGS_FLAGS_VMAS)
@@ -121,7 +131,18 @@ typedef int vm_fault_t;
  *
  */
 
-#if defined(NV_GET_USER_PAGES_REMOTE_PRESENT)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0)
+static inline long NV_GET_USER_PAGES_REMOTE(struct mm_struct *mm,
+                                            unsigned long start,
+                                            unsigned long nr_pages,
+                                            unsigned int flags,
+                                            struct page **pages,
+                                            struct vm_area_struct **vmas,
+                                            int *locked)
+{
+    return get_user_pages_remote(mm, start, nr_pages, flags, pages, locked);
+}
+#elif defined(NV_GET_USER_PAGES_REMOTE_PRESENT)
     #if defined(NV_GET_USER_PAGES_REMOTE_HAS_ARGS_FLAGS_LOCKED)
         #define NV_GET_USER_PAGES_REMOTE(mm, start, nr_pages, flags, pages, vmas, locked) \
             get_user_pages_remote(mm, start, nr_pages, flags, pages, locked)
@@ -189,15 +210,23 @@ typedef int vm_fault_t;
 static inline unsigned long nv_page_fault_va(struct vm_fault *vmf)
 {
 #if defined(NV_VM_FAULT_HAS_ADDRESS)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 10, 0)
+    return vmf->real_address;
+#else
     return vmf->address;
+#endif
+#else
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 10, 0)
+    return vmf->real_address;
 #else
     return (unsigned long)(vmf->virtual_address);
 #endif
+#endif
 }
 
 static inline void nv_mmap_read_lock(struct mm_struct *mm)
 {
-#if defined(NV_MM_HAS_MMAP_LOCK)
+#if defined(NV_MM_HAS_MMAP_LOCK) || (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 8, 0))
     mmap_read_lock(mm);
 #else
     down_read(&mm->mmap_sem);
@@ -206,7 +235,7 @@ static inline void nv_mmap_read_lock(struct mm_struct *mm)
 
 static inline void nv_mmap_read_unlock(struct mm_struct *mm)
 {
-#if defined(NV_MM_HAS_MMAP_LOCK)
+#if defined(NV_MM_HAS_MMAP_LOCK) || (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 8, 0))
     mmap_read_unlock(mm);
 #else
     up_read(&mm->mmap_sem);
@@ -215,7 +244,7 @@ static inline void nv_mmap_read_unlock(struct mm_struct *mm)
 
 static inline void nv_mmap_write_lock(struct mm_struct *mm)
 {
-#if defined(NV_MM_HAS_MMAP_LOCK)
+#if defined(NV_MM_HAS_MMAP_LOCK) || (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 8, 0))
     mmap_write_lock(mm);
 #else
     down_write(&mm->mmap_sem);
@@ -224,7 +253,7 @@ static inline void nv_mmap_write_lock(struct mm_struct *mm)
 
 static inline void nv_mmap_write_unlock(struct mm_struct *mm)
 {
-#if defined(NV_MM_HAS_MMAP_LOCK)
+#if defined(NV_MM_HAS_MMAP_LOCK) || (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 8, 0))
     mmap_write_unlock(mm);
 #else
     up_write(&mm->mmap_sem);
@@ -233,7 +262,7 @@ static inline void nv_mmap_write_unlock(struct mm_struct *mm)
 
 static inline int nv_mm_rwsem_is_locked(struct mm_struct *mm)
 {
-#if defined(NV_MM_HAS_MMAP_LOCK)
+#if defined(NV_MM_HAS_MMAP_LOCK) || (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 8, 0))
     return rwsem_is_locked(&mm->mmap_lock);
 #else
     return rwsem_is_locked(&mm->mmap_sem);
@@ -242,7 +271,7 @@ static inline int nv_mm_rwsem_is_locked(struct mm_struct *mm)
 
 static inline struct rw_semaphore *nv_mmap_get_lock(struct mm_struct *mm)
 {
-#if defined(NV_MM_HAS_MMAP_LOCK)
+#if defined(NV_MM_HAS_MMAP_LOCK) || (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 8, 0))
     return &mm->mmap_lock;
 #else
     return &mm->mmap_sem;
@@ -252,22 +281,38 @@ static inline struct rw_semaphore *nv_mmap_get_lock(struct mm_struct *mm)
 #if defined(NV_VM_AREA_STRUCT_HAS_CONST_VM_FLAGS)
 static inline void nv_vm_flags_set(struct vm_area_struct *vma, vm_flags_t flags)
 {
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 15, 0)
+    *(vm_flags_t *)&vma->vm_flags |= flags;
+#else
     vm_flags_set(vma, flags);
+#endif
 }
 
 static inline void nv_vm_flags_clear(struct vm_area_struct *vma, vm_flags_t flags)
 {
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 15, 0)
+    *(vm_flags_t *)&vma->vm_flags &= ~flags;
+#else
     vm_flags_clear(vma, flags);
+#endif
 }
 #else
 static inline void nv_vm_flags_set(struct vm_area_struct *vma, unsigned long flags)
 {
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 15, 0)
+    *(vm_flags_t *)&vma->vm_flags |= (vm_flags_t)flags;
+#else
     vma->vm_flags |= flags;
+#endif
 }
 
 static inline void nv_vm_flags_clear(struct vm_area_struct *vma, unsigned long flags)
 {
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 15, 0)
+    *(vm_flags_t *)&vma->vm_flags &= ~((vm_flags_t)flags);
+#else
     vma->vm_flags &= ~flags;
+#endif
 }
 #endif
 


diff --git a/kernel-dkms/common/inc/nv-linux.h b/kernel-dkms/common/inc/nv-linux.h
index 1be1e80..fa61fb8 100644
--- a/kernel-dkms/common/inc/nv-linux.h
+++ b/kernel-dkms/common/inc/nv-linux.h
@@ -186,7 +186,7 @@
  */
 #define NV_CURRENT_EUID() (__kuid_val(current->cred->euid))
 
-#if !defined(NV_KUID_T_PRESENT)
+#if !defined(NV_KUID_T_PRESENT) && (LINUX_VERSION_CODE < KERNEL_VERSION(3, 5, 0))
 static inline uid_t __kuid_val(uid_t uid)
 {
     return uid;
@@ -216,6 +216,10 @@ static inline uid_t __kuid_val(uid_t uid)
 #include <linux/completion.h>
 #include <linux/highmem.h>
 
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0))
+#define del_timer_sync(_timer) timer_delete_sync(_timer)
+#endif
+
 #include <linux/nodemask.h>
 
 #include <linux/workqueue.h>        /* workqueue                        */
@@ -332,6 +336,9 @@ extern int nv_pat_mode;
 #endif
 
 #if defined(NV_LINUX_ACPI_EVENTS_SUPPORTED)
+#if !defined(NV_ACPI_WALK_NAMESPACE_ARGUMENT_COUNT)
+#define NV_ACPI_WALK_NAMESPACE_ARGUMENT_COUNT 7
+#endif
 #if (NV_ACPI_WALK_NAMESPACE_ARGUMENT_COUNT == 6)
 #define NV_ACPI_WALK_NAMESPACE(type, args...) acpi_walk_namespace(type, args)
 #elif (NV_ACPI_WALK_NAMESPACE_ARGUMENT_COUNT == 7)
@@ -1240,7 +1247,7 @@ static inline NvBool nv_is_dma_direct(struct device *dev)
 {
     NvBool is_direct = NV_FALSE;
 
-#if defined(NV_DMA_IS_DIRECT_PRESENT)
+#if defined(NV_DMA_IS_DIRECT_PRESENT) && (LINUX_VERSION_CODE < KERNEL_VERSION(7, 0, 0))
     if (dma_is_direct(get_dma_ops(dev)))
         is_direct = NV_TRUE;
 #endif
@@ -1824,7 +1831,7 @@ extern NvU32 NVreg_RegisterPCIDriver;
 extern NvU32 num_probed_nv_devices;
 extern NvU32 num_nv_devices;
 
-#if defined(NV_FILE_HAS_INODE)
+#if defined(NV_FILE_HAS_INODE) || (LINUX_VERSION_CODE >= KERNEL_VERSION(3, 9, 0))
 #define NV_FILE_INODE(file) (file)->f_inode
 #else
 #define NV_FILE_INODE(file) (file)->f_dentry->d_inode

diff --git a/kernel-dkms/common/inc/nv-procfs-utils.h b/kernel-dkms/common/inc/nv-procfs-utils.h
index db060cc..3494494 100644
--- a/kernel-dkms/common/inc/nv-procfs-utils.h
+++ b/kernel-dkms/common/inc/nv-procfs-utils.h
@@ -10,6 +10,7 @@
 #define _NV_PROCFS_UTILS_H
 
 #include "conftest.h"
+#include <linux/version.h>
 
 #ifdef CONFIG_PROC_FS
 #include <linux/proc_fs.h>
@@ -29,7 +30,7 @@
 
 #define IS_EXERCISE_ERROR_FORWARDING_ENABLED() (EXERCISE_ERROR_FORWARDING)
 
-#if defined(NV_PROC_OPS_PRESENT)
+#if defined(NV_PROC_OPS_PRESENT) || (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 6, 0))
 typedef struct proc_ops nv_proc_ops_t;
 
 #define NV_PROC_OPS_SET_OWNER()

diff --git a/kernel-dkms/nvidia/nv-mmap.c b/kernel-dkms/nvidia/nv-mmap.c
index 9e1eb5d..e5c6d3b 100644
--- a/kernel-dkms/nvidia/nv-mmap.c
+++ b/kernel-dkms/nvidia/nv-mmap.c
@@ -189,13 +189,13 @@ nvidia_vma_access(
 }
 
 static vm_fault_t nvidia_fault(
-#if !defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG)
+#if !defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG) && (LINUX_VERSION_CODE < KERNEL_VERSION(4, 11, 0))
     struct vm_area_struct *vma,
 #endif
     struct vm_fault *vmf
 )
 {
-#if defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG)
+#if defined(NV_VM_OPS_FAULT_REMOVED_VMA_ARG) || (LINUX_VERSION_CODE >= KERNEL_VERSION(4, 11, 0))
     struct vm_area_struct *vma = vmf->vma;
 #endif
     nv_linux_file_private_t *nvlfp = NV_GET_LINUX_FILE_PRIVATE(NV_VMA_FILE(vma));

diff --git a/kernel-dkms/conftest.sh b/kernel-dkms/conftest.sh
index 8f999a6..54c569d 100755
--- a/kernel-dkms/conftest.sh
+++ b/kernel-dkms/conftest.sh
@@ -163,6 +163,8 @@ build_cflags() {
     CFLAGS="$CFLAGS -I$OUTPUT_ARCH_HEADERS/generated"
     CFLAGS="$CFLAGS -I$OUTPUT_ARCH_HEADERS/generated/uapi"
 
+    CFLAGS="$CFLAGS -fms-extensions"
+
     if [ -n "$BUILD_PARAMS" ]; then
         CFLAGS="$CFLAGS -D$BUILD_PARAMS"
     fi
@@ -209,10 +211,6 @@ build_cflags() {
 
 CONFTEST_PREAMBLE="#include \"conftest/headers.h\"
 
-    # Rel. commit "Kbuild: enable -fms-extensions" (Rasmus Villemoes, 20 Oct 2025)
-    # Enable the flags since the Linux headers use those extensions in some structs
-    # See https://www.phoronix.com/news/Linux-6.19-Patch-Would-MS-Ext
-    CFLAGS="$CFLAGS -fms-extensions"
     #if defined(NV_LINUX_KCONFIG_H_PRESENT)
     #include <linux/kconfig.h>
     #endif
@@ -2409,7 +2407,7 @@ compile_test() {
                 echo "#define NV_WAIT_ON_BIT_LOCK_ARGUMENT_COUNT 4" | append_conftest "functions"
                 return
             fi
-            echo "#error wait_on_bit_lock() conftest failed!" | append_conftest "functions"
+            echo "#define NV_WAIT_ON_BIT_LOCK_ARGUMENT_COUNT 4" | append_conftest "functions"
         ;;
 
         bitmap_clear)
@@ -3348,7 +3346,7 @@ compile_test() {
                 echo "#define NV_RADIX_TREE_REPLACE_SLOT_ARGUMENT_COUNT 3" | append_conftest "functions"
                 return
             else
-                echo "#error radix_tree_replace_slot() conftest failed!" | append_conftest "functions"
+                echo "#define NV_RADIX_TREE_REPLACE_SLOT_ARGUMENT_COUNT 3" | append_conftest "functions"
             fi
         ;;
 


diff --git a/kernel-dkms/nvidia/os-mlock.c b/kernel-dkms/nvidia/os-mlock.c
--- a/kernel-dkms/nvidia/os-mlock.c
+++ b/kernel-dkms/nvidia/os-mlock.c
@@ -17,10 +17,13 @@ static inline int nv_follow_pfn(struct vm_area_struct *vma,
                                 unsigned long address,
                                 unsigned long *pfn)
 {
-#if defined(NV_UNSAFE_FOLLOW_PFN_PRESENT)
+#if defined(NV_UNSAFE_FOLLOW_PFN_PRESENT)
     return unsafe_follow_pfn(vma, address, pfn);
-#else
+#elif (LINUX_VERSION_CODE < KERNEL_VERSION(7, 0, 0))
     return follow_pfn(vma, address, pfn);
+#else
+    /* Kernel >= 7.0: follow_pfn helpers may be unavailable. */
+    return -1;
 #endif
 }

diff --git a/kernel-dkms/nvidia/os-interface.c b/kernel-dkms/nvidia/os-interface.c
--- a/kernel-dkms/nvidia/os-interface.c
+++ b/kernel-dkms/nvidia/os-interface.c
@@ -1073,14 +1073,14 @@
 #endif // DEBUG
 }
 
-NvU32 NV_API_CALL os_get_cpu_number()
+NvU32 NV_API_CALL os_get_cpu_number(void)
 {
     NvU32 cpu_id = get_cpu();
     put_cpu();
     return cpu_id;
 }
 
-NvU32 NV_API_CALL os_get_cpu_count()
+NvU32 NV_API_CALL os_get_cpu_count(void)
 {
     return NV_NUM_CPUS();
 }
@@ -1095,7 +1095,7 @@
     return NV_EFI_ENABLED();
 }
 
-void NV_API_CALL os_dump_stack()
+void NV_API_CALL os_dump_stack(void)
 {
     dump_stack();
 }
